#include<iostream>
using namespace std;



int main(){
    //we can write if(head==nullptr) as if(head==0)
    //we can write if(head!=nullptr) as if(head)
    //if we want to write that the list1 has some nodes- if(list1)

    //for example
    //if(head) and if(head!=nullptr) has thse same meaning just need to understand that if(head)
    //tries to tell us that head has data and is not null
}